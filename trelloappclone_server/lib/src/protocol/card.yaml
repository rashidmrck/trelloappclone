class: Card
table: card
fields:
  listId: int, parent=listboard
  userId: int, parent=user
  name: String
  description: String?
  startDate: DateTime?
  dueDate: DateTime?
  attachment: bool?
  archived: bool?
  checklist: bool?
  comments: bool?
